[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (may require system dependencies)
    windows: marks tests as Windows-specific
    slow: marks tests as slow running
addopts = 
    --strict-markers
    --disable-warnings
    -v